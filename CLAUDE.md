# CLAUDE.md — הנחיות לפרויקט "אתר הרצאות"

קובץ זה נטען אוטומטית בכל סשן ומשמש כזיכרון קבוע לפרויקט. עדכנו אותו כשמשהו מהותי משתנה.

## מה זה הפרויקט
אתר תדמית **סטטי** ודו-לשוני (עברית 🇮🇱 RTL + אנגלית 🇬🇧 LTR) למרצה.
אין build, אין תלויות, אין `node_modules`. פותחים את `index.html` בדפדפן וזהו.

## הרצה מקומית
- הכי פשוט: לחיצה כפולה על `index.html`.
- מומלץ (כדי שווידאו/תמונות ייטענו נכון): `python -m http.server 8000` ואז `http://localhost:8000`.

## מבנה הקבצים
- `index.html` — שלד הדף. הטקסטים מסומנים ב-`data-i18n="key"` ומוחלפים בריצה.
- `css/styles.css` — כל העיצוב.
- `js/translations.js` — **מקור האמת לתוכן.** כאן עורכים טקסטים, כרטיסים, מחירים, רילסים ופרטי קשר.
- `js/main.js` — לוגיקה: החלפת שפה, הזרקת תוכן דינמי, אנימציות, טופס.
- `assets/images/`, `assets/videos/` — מדיה (כרגע placeholders עם `.gitkeep`).

## עקרונות עבודה בפרויקט הזה
- **כל תוכן נערך ב-`js/translations.js`** — תמיד בשתי השפות (`he` ו-`en`) יחד. אל תשאירו שפה אחת מאחור.
- כרטיסי הרצאות (`cards.lectures`), מחירים (`PRICING`), ורילסים (`REELS`) מוזרקים מ-JS אל ה-DOM — לכן הם דו-לשוניים ומתעדכנים אוטומטית. אל תכתבו אותם ידנית ב-HTML.
- שמירת שפה נעשית ב-`localStorage` תחת `siteLang`.
- ה-`dir` (rtl/ltr) וה-`lang` מוחלפים אוטומטית ב-`applyLang()`.
- מספר וואטסאפ: בפורמט בינלאומי **בלי** `+` (למשל `972501234567`), בתוך `CONTACT.whatsapp`.
- מחיר `null` ב-`PRICING` => מוצג "בהתאמה"/"Custom". `popular: true` => תג "הכי פופולרי".
- Placeholders שצריך להחליף מסומנים בקוד בהערות `TODO`.

## עבודה עם Git ו-GitHub
- ריפו מרוחק ב-GitHub תחת חשבון **OC0h3n** (פרוטוקול HTTPS, דרך `gh`).
- זרימה: עריכה → `git add` → `git commit` → `git push`. רק `push` שולח החוצה ל-GitHub.
- העלאה לאוויר: האתר סטטי, אז מתאים ל-GitHub Pages / Netlify / Vercel.

## פורמט הודעות קומיט
- הודעה קצרה וברורה בעברית או אנגלית, מתארת *מה* השתנה.
