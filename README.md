# אתר הרצאות — מדריך עריכה 🎤

אתר תדמית דו-לשוני (עברית 🇮🇱 + אנגלית 🇬🇧) למרצה. אתר **סטטי** לחלוטין — בלי התקנות, בלי build. פשוט פותחים את `index.html` בדפדפן.

## הרצה מקומית

- **הכי פשוט:** לחיצה כפולה על `index.html`.
- **מומלץ (כדי שווידאו ותמונות ייטענו נכון):** הרצת שרת מקומי קטן מתוך תיקיית הפרויקט:
  ```
  python -m http.server 8000
  ```
  ואז פותחים בדפדפן: `http://localhost:8000`

---

## איפה עורכים מה

### ✏️ טקסטים (כותרות, פסקאות, כפתורים)
הכל בקובץ **`js/translations.js`** — אובייקט `TRANSLATIONS` עם `he` (עברית) ו-`en` (אנגלית).
מצאו את המפתח, שנו את הערך בשתי השפות. זהו.

### 🎓 ההרצאות (כרטיסים)
ב-`js/translations.js`, בתוך `cards.lectures` (יש גרסה לכל שפה). כל כרטיס:
```js
{ icon: "🚀", title: "כותרת", desc: "תיאור קצר", tag: "נוער" }
```
אפשר להוסיף/למחוק כרטיסים — הם מתעדכנים אוטומטית.

### 💰 מחירים והנחות
ב-`js/translations.js`, במערך **`PRICING`**. לכל חבילה: `price` (מספר), `currency`, `name`, `desc`, ו-`features`.
- מחיר `null` = מציג "בהתאמה" (במקום מספר).
- `popular: true` = מסמן את החבילה כ"הכי פופולרי".
- טקסט ההנחות הכללי נמצא במפתח `pricing.note` (בשתי השפות).

### 🎬 רילסים / סרטונים
1. שימו קבצי `.mp4` בתיקייה **`assets/videos/`**.
2. ב-`js/translations.js`, במערך **`REELS`**, עדכנו את `src`:
   ```js
   { src: "assets/videos/reel1.mp4", poster: "assets/images/reel1.jpg", title: { he: "...", en: "..." } }
   ```
   - `poster` (אופציונלי) = תמונת תצוגה לפני הנגינה.
   - כל עוד `src` ריק — מוצג רקע צבעוני כ-placeholder.

### 🖼️ תמונת המרצה
שימו תמונה בשם `speaker.jpg` בתיקייה `assets/images/`, ואז ב-`index.html` החליפו את ה-placeholder
(`<div class="about__photo-placeholder">📷</div>`) ב-`<img src="assets/images/speaker.jpg" alt="..." />`.

### 📞 וואטסאפ ופרטי קשר
- מספר הוואטסאפ: ב-`js/translations.js`, באובייקט **`CONTACT`**, שדה `whatsapp` (פורמט בינלאומי **בלי** `+`, למשל `972501234567`).
- טלפון/מייל/רשתות חברתיות בתחתית: ב-`index.html` בתוך ה-`<footer>`.

---

## העלאה לאוויר (כשתהיו מוכנים)
האתר סטטי, אז אפשר להעלות חינם לכל אחד מאלה:
- **GitHub Pages** — דחיפת התיקייה לריפו והפעלת Pages.
- **Netlify / Vercel** — גרירת התיקייה לאתר שלהם, וזהו.

> כל ה-placeholders מסומנים בקוד בהערות `TODO`. חפשו "TODO" כדי לראות מה כדאי להחליף.
